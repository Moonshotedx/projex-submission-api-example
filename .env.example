# ProjeX host only — no trailing /api/v1
PROJEX_API_URL=http://localhost:3000

# From Account → API keys (shown once). Needs cohorts:read, milestones:read, submissions:write
PROJEX_API_KEY=pjx_live_…
